# Team Task Manager — API

Express + Mongoose API for the Team Task Manager.

```bash
cp .env.example .env
npm install
npm run seed     # demo data
npm run dev      # http://localhost:5000
```

See the project root [README](../README.md) for the full API reference, schemas, and edge-case rules.
