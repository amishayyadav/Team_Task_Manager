# Team Task Manager — Client

React + Vite frontend.

```bash
cp .env.example .env
npm install
npm run dev    # http://localhost:5173
```

The API base URL is `VITE_API_URL` (defaults to `http://localhost:5000/api`). See the project root [README](../README.md) for everything else.
